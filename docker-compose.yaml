version: '3.7'

services:
  lemonbot:
    image: heavysaturn/lemonbot:latest
    container_name: lemon-server-bot
    command: pipenv run bot
    restart: always
    environment:
      - LEMON_DISCORD_TOKEN=<supersecrettokenyouguise>
